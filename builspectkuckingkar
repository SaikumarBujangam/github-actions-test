resource "aws_cloudwatch_metric_alarm" "search_request_latency" {
  alarm_name          = "search-request-latency-high"
  comparison_operator = "GreaterThanThreshold"
  evaluation_periods  = "1"
  metric_name         = "SearchRequestLatency"  # Assuming this metric is available
  namespace           = "AWS/OpenSearchServerless"  # Hypothetical namespace
  period              = "60"
  statistic           = "Average"
  threshold           = "1.0"

  # Assuming dimensions or identifiers for serverless resources are available
  # dimensions = {
  #   ResourceId = aws_opensearch_serverless_resource.example.id  # Hypothetical reference
  # }

  alarm_description = "This metric monitors high search request latency"
  actions_enabled   = true

  # Specify any actions
}

# Similarly define alarms for SearchOCU and SearchRequestRate if available
