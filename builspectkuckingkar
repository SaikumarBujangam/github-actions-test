import math

def factorial(n):
  if n==0:
    return 1
  else:
    return n*factorial(n-1)

def log10(num):
  return math.log10(num)

def degrees_to_radians(deg):
  return math.radians(deg)

def trigonometry(deg):
  rad = degrees_to_radians(deg)
  return math.sin(rad), math.cos(rad), math.tan(deg)
