def fibonacci():
    count = int(input("Enter the number of Fibonacci terms you want: "))
    a, b = 0, 1
    for _ in range(count):
        yield a
        a, b = b, a + b

# Example usage:
# Call the generator and print the Fibonacci numbers
for num in fibonacci():
    print(num)
