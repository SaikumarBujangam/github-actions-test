variable "global_secondary_indexes" {
  type = list(object({
    name            = string
    hash_key        = string
    range_key       = string
    projection_type = string
    // Include these if using provisioned capacity
    // read_capacity_units  = number
    // write_capacity_units = number
  }))
  default = []
}
