Sonarjob:
  stage: Sonarjob  
  tags: ["gen10"]
  only:
    variables:
      - $BUILD_TO == "dev"
    #- casino2.1-dev
   #- $CI_COMMIT_BRANCH == "casino20-dev" && $SONAR == "scan"
  script:
   #- npm run test
    - pwd
    - ls -la
    #- cd coverage
    #- ls -la
    - npm install -g sonarqube-scanner
    - sonar-scanner -v 
    - ls -la
    #- curl -v http://sonar.intranet.roche.com/batch/index
    - sonar-scanner -Dsonar.host.url=$SONAR_URL -Dsonar.login=$SONAR_LOGIN -Dsonar.projectKey=redcap-webapp -Dproject.settings=sonar-project.properties -Dsonar.verbose=true
    #- cd coverage
   #- Dsonar.es.bootstrap.checks.disable=$SONAR_ES_BOOTSTRAP_CHECKS_DISABLE
    - ls -la
