This workflow is working perfectly fine and handling the job dependency perfectly.

# DO NOT MODIFY OR DELETE THIS WORKFLOW!!

name: ADL CICD Rollback Pipeline
on:
  workflow_dispatch:
    inputs:
      deploy_env:
        description: 'Deployment Environment'
        required: true
        type: choice
        options:
          - 'dev'
          - 'qa'
          - 'qc'  
          - 'prod'
      repo_tag:
        description: 'Repo Tag (e.g., r1.0.0)'
        required: true
        type: string

jobs:

  adl-cicd-qc-prod-approval:
    if: github.event_name == 'workflow_dispatch' && (github.event.inputs.deploy_env == 'qc' || github.event.inputs.deploy_env == 'prod')
    runs-on: ubuntu-latest
    environment: hotfix
    steps:
      - name: Await Rollback Deployment Approval in Higher Lifecycles
        run: echo "Awaiting approval for Rollback deployment in higher lifecycles . . ."  
        
  adl-cicd-rollback-dev-qa-phase:
    if: github.event_name == 'workflow_dispatch' && (github.event.inputs.deploy_env == 'dev' || github.event.inputs.deploy_env == 'qa')
    uses: AlightEngineering/adl-awsdeploy-cicd/.github/workflows/adl-cicd-rollback-repo-phase.yml@feature/workflowrefactor
    secrets: inherit
    with:
      event_repo_name: ${{ github.repository }}
      event_repo_branch: ${{ github.ref_name }}
      deploy_env: ${{ github.event.inputs.deploy_env }}
      repo_tag: ${{ github.event.inputs.repo_tag }}

  adl-cicd-rollback-qc-prod-phase:
    if: github.event_name == 'workflow_dispatch' && (github.event.inputs.deploy_env == 'qc' || github.event.inputs.deploy_env == 'prod')
    needs: adl-cicd-qc-prod-approval
    uses: AlightEngineering/adl-awsdeploy-cicd/.github/workflows/adl-cicd-rollback-repo-phase.yml@feature/workflowrefactor
    secrets: inherit
    with:
      event_repo_name: ${{ github.repository }}
      event_repo_branch: ${{ github.ref_name }}
      deploy_env: ${{ github.event.inputs.deploy_env }}
      repo_tag: ${{ github.event.inputs.repo_tag }}
      
  adl-log-event:
    runs-on: ubuntu-latest
    steps:
      - name: Default
        run: |
          echo "Deployment Environemnt: ${{ github.event.inputs.deploy_env }}"
          echo "Deployment Repo Tag: ${{ github.event.inputs.repo_tag }}"

but the below workflow is not handling the job dependency correctly. why?

# DO NOT MODIFY OR DELETE THIS WORKFLOW!!

name: ADL CICD Deployment Pipeline

on:
  pull_request:
    branches: [ master ]
    types: [opened, reopened, edited]
  push:
    branches: [master, feature/*, hotfix/* ]
    paths-ignore:
      - '.version'
      - '.github/**'
      - 'Explore/**'
      - 'README.md'
      - 'CODEOWNERS'
      - '.group'
  workflow_dispatch:
    inputs:
      deploy_env:
        description: 'Deployment Environment (e.g., qa, qc)'
        required: true
        type: choice
        options:
          - 'qa'
          - 'qc'    
      feature_branch_name:
        description: 'Enter the branch (e.g., feature/your-branch-name)'
        required: true
        default: 'feature/your-branch-name'  
jobs:
  adl-cicd-check-pull-request:
    if: github.event_name == 'pull_request'
    uses: AlightEngineering/adl-awsdeploy-cicd/.github/workflows/adl-cicd-check-pull-request.yml@feature/workflowrefactor
    secrets: inherit

  adl-cicd-deploy-phase:
    if: github.event_name == 'push' && (startsWith(github.ref, 'refs/heads/hotfix/') != true )|| (github.event_name == 'workflow_dispatch' && startsWith(github.event.inputs.feature_branch_name, 'feature/'))
    uses: AlightEngineering/adl-awsdeploy-cicd/.github/workflows/adl-cicd-deploy-repo-phase.yml@feature/workflowrefactor
    secrets: inherit
    with:
      event_repo_name: ${{ github.repository }}
      event_repo_branch: ${{ github.ref_name }}
      deploy_env: ${{ github.event_name == 'push' && github.ref == 'refs/heads/master' && 'prod' || github.event_name == 'workflow_dispatch' && github.event.inputs.deploy_env || github.event_name == 'push' && startsWith(github.ref, 'refs/heads/feature/') && 'dev' }}

  adl-cicd-hotfix-approval:
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/heads/hotfix/')
    runs-on: ubuntu-latest
    environment: hotfix
    steps:
      - name: Await Hotfix Deployment Approval
        run: echo "Awaiting approval for hotfix deployment . . ." 
          
  adl-cicd-hotfix-phase:
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/heads/hotfix/')
    needs: adl-cicd-hotfix-approval
    uses: AlightEngineering/adl-awsdeploy-cicd/.github/workflows/adl-cicd-deploy-repo-phase.yml@feature/workflowrefactor
    secrets: inherit
    with:
      event_repo_name: ${{ github.repository }}
      event_repo_branch: ${{ github.ref_name }}
      deploy_env: prod

  adl-log-event:
    runs-on: ubuntu-latest
    steps:
      - name: GitHub Event Name
        run: echo ${{ github.event_name }}
      - name: Check Feature Branch 
        if: github.event_name == 'workflow_dispatch'
        run: |
          if [[ "${{ github.event.inputs.feature_branch_name }}" != feature/* ]]; then
            echo "Error: The feature_branch_name is an invalid branch and must start with 'feature/'."
            exit 1
          fi
