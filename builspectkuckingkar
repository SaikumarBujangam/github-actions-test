dynamic "attribute" {
  for_each = [for gsi in var.global_secondary_indexes : gsi.range_key != null ? gsi.range_key : "dummy_key"]
  content {
    name = attribute.value
    type = attribute.value != "dummy_key" ? "S" : null
  }
}
