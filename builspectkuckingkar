data "aws_lb" "ingress_load_balancer" {
  depends_on = [helm_release.my-discourse-0]

  tags = {
    "elbv2.k8s.aws/cluster" = "${var.org_key}-discourse-cluster"
    "ingress.k8s.aws/stack" = "default/${helm_release.my-discourse-0.name}"
    "ingress.k8s.aws/resource" = "LoadBalancer"
  }
}
