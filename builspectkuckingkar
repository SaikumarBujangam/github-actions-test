resource "local_file" "config" {
  content = templatefile("${path.module}/template/config-template.tpl", {
    region = var.region,
    x_navify_tenant=var.x_navify_tenant,
    okta_config=var.okta_config
  })
  filename = "${path.module}/python/config.py"
}
