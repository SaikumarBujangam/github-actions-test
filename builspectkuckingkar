dynamic "attribute" {
  for_each = var.global_secondary_indexes != null ? flatten([for gsi in var.global_secondary_indexes : [gsi.range_key]]) : []
  content {
    name = attribute.value
    type = "S" # Or any other default type you want
  }
}
