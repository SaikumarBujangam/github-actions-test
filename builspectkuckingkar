steps:
  - name: Checkout a specific repository
    uses: actions/checkout@v3
    with:
      repository: 'owner/repo-name'
      token: ${{ secrets.GITHUB_TOKEN }} # Use a PAT if accessing a private repository outside of your current account
