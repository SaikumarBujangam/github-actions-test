\resource "aws_sagemaker_user_profile" "sagemaker_user_profile" {
  
  user_profile_name         = "${var.project_key}-${var.rstudio_user_name}"
  domain_id                 = var.sagemaker_domain_id
  user_settings {
    execution_role = data.aws_iam_role.rstudio_user_role.arn

    r_studio_server_pro_app_settings {
      user_group = "R_STUDIO_USER"
    }
      /*canvas_app_settings {
      direct_deploy_settings {
        status = "ENABLED"
      }


    }*/

    canvas_app_settings {
      kendra_settings = "ENABLED"


    }

kendra_settings


    }
  tags = merge(
    {
      "project" = "${var.project_key}"
    }, 
    {
      "platform" = "${var.platform}"
    }, 
    {
      "organization" = "${var.org_key}"
    }, 
  )
}
 ╷
│ Error: Argument or block definition required
│
│   on ..\infra-scripts\user-infra-services\sagemaker-user-creation\main.tf line 25, in resource "aws_sagemaker_user_profile" "sagemaker_user_profile":
│   25: kendra_settings
│
│ An argument or block definition is required here. To set an argument, use the equals sign "=" to introduce the argument value.
