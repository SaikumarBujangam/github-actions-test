# DO NOT MODIFY OR DELETE THIS WORKFLOW!!

name: ADL CICD Initiate Deployment

on:
  pull_request:
    branches: [ master, release/qa, release/qc, release/prod ]
    types: [opened, reopened, edited]
  push:
    branches: [master, release/qa, release/qc, release/prod ]
    paths-ignore:
      - '.version'
      - '.github/**'
      - 'Explore/**'
      - 'README.md'
      - 'CODEOWNERS'
      - '.group'
      
jobs:
  adl-cicd-check-pull-request:
    if: github.event_name == 'pull_request'
    uses: AlightEngineering/adl-awsdeploy-cicd/.github/workflows/adl-cicd-check-pull-request.yml@master
    secrets:
      envPAT: ${{ secrets.REPO_PAT }}

  adl-cicd-check-pull-request-hotfix:
    if: github.event_name == 'pull_request'
    uses: AlightEngineering/adl-awsdeploy-cicd/.github/workflows/adl-cicd-check-pull-request-hotfix.yml@master
    secrets:
      envPAT: ${{ secrets.REPO_PAT }}

  adl-cicd-checkout-repo-phase:
    if: github.event_name == 'push'
    uses: AlightEngineering/adl-awsdeploy-cicd/.github/workflows/adl-cicd-checkout-repo-phase.yml@master
    secrets:
      envPAT: ${{ secrets.REPO_PAT }}

  adl-log-event:
    runs-on: ubuntu-latest
    steps:
      - name: Default
        run: |
          echo ${{ github.event_name }}
